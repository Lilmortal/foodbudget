export interface Spacing {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
}

const spacing: Spacing = {
  xs: '10px',
  sm: '10px',
  md: '10px',
  lg: '10px',
  xl: '10px',
};

export default spacing;
